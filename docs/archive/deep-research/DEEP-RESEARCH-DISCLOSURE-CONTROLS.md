# Disclosure controls — bounded follow-up, September 22, 2026

> Historical delivery / validation record. Preserve its evidence and decisions;
> dated next steps and run allowances are not current instructions or reusable approval.
> Use the [current roadmap](../../DEEP-RESEARCH-STATUS.md) for active work and
> the [archive index](README.md) to navigate history.

Follow-up: the [retrospective review and bound-disclosure repair](DEEP-RESEARCH-REVIEW-DISCLOSURES.md)
records issues found after this PR's initial review. Stricter read-only rescoring
preserves the live observations below; new diagnostic plans bind the corrected
runtime and evaluation contract. Historical artifacts remain unchanged.

The [first paired diagnostic](DEEP-RESEARCH-COVERAGE-DIAGNOSTIC.md) found a
financial-draft false closure despite exact quotations. Its harness is integrated
by PR #20 (`bd604c7`) into `codex/deep-research-v2`. Main is unchanged; the merged
`codex/coverage-paired-diagnostic` branch was removed locally and remotely.

## Purpose and protocol

Develop on `codex/coverage-disclosure-controls`, with a separate PR targeting the
feature branch. This increment adds evaluation controls and a six-call campaign,
not a production semantic classifier, report repair or default-policy change.

The offline evaluation set retains the exact issue inventory and original reader,
then creates three variants for the `case.review.draft` obligation:

1. Original general report status only: expected unresolved.
2. Original reader plus a statement that the conditional operating package was
   reviewed only for operating inputs/calculations: expected unresolved.
3. Variant 2 plus an explicit statement that the financial schedules remain an
   unreviewed draft and importing them is not approval: expected reader-covered.

The positive control does not require the internal phrase “Stage 2.” Both edited
variants use the same neutral review-scope heading. Labels and grading rationales
are kept outside the provider payload. These are synthetic evaluation additions,
not source facts or modifications to an accepted report. The unchanged original
already has the first diagnostic's live observations; it is not dispatched again
in this campaign.

Live order on the same 18 frozen issues, using the unchanged Sol/high verifier:

| Reader control | First | Second | Third |
| --- | --- | --- | --- |
| Operating review only (negative) | Packed 18 | Legacy 12 | Legacy 6 |
| Explicit financial draft (positive) | Legacy 12 | Legacy 6 | Packed 18 |

This balances policy order **across different controls**, not within each one.
Reader condition, warm-up, order and model variation still confound a causal
speed claim. Four target-containing calls provide four semantic observations;
the two legacy-six calls check the remaining issues, not additional target trials.
The intended result is correct negative/positive discrimination under each policy,
not mere policy agreement or full-report acceptance.

## Authorized bounds and fail-closed behavior

The user approved this next sequence and reasonable bounded follow-ups. The
recorded campaign limits are six calls, 900 seconds overall, 350,000 tokens
best-effort aggregate admission/post-call enforcement, and 600 seconds per call.
The larger token reserve covers two complete matched comparisons. Worker 880s
and supervisor 890s preserve cleanup headroom. There is one allowance for all six
calls, not a reset between controls. Do not extend the deadline to finish a pair.

No retry, fallback, automatic renewal, report export, historical edit or reusable
attestation is allowed. Unknown usage stops dispatch and remains unknown. Each
capsule is fresh and single-use, bound to source hashes, runtime/code revision,
full payloads and the exact plan digest. Old three-call plans retain their
separate original contract. The existing OpenAI Codex destination and disclosed
reader/issue scope remain unchanged apart from the stated synthetic paragraphs.

## Offline gates and interpretation

Targeted tests cover the control transformations, unchanged issues, no expected
label leakage, exact source/reader bindings, missing/duplicate decisions, malformed
quotes, wrong decisions, unreviewed responses, budget exhaustion, shared deadlines,
incomplete usage and old-plan compatibility. Mock replies test the harness, not
model semantic competence.

A correct negative answer can include a critical missing-disclosure finding;
that is expected detection, not a failed control or an accepted reader. All
findings remain in the raw/checked review. A positive answer must also pass the
ordinary exact-witness and affected-finding checks. Success on these narrow
synthetic controls does not establish that the real writer consistently supplies
the disclosure, or that other material limitations are semantically covered.

Keep the legacy default and all financial acceptance gates unchanged. The results
below do not support proceeding directly to a full research acceptance run.

Pre-live verification: 72 affected diagnostic/payload/engine/model-service tests
passed, followed by the final 30-test control/helper scope after scoring fixes
(overlapping groups, not additive). Lint and whitespace checks pass. The actual
historical v1 plan still validates; replaying its saved replies through the scorer
correctly flags legacy's false closure and accepts packed's unresolved negative.
Terra/medium implemented the controls. Independent Sol/high review reports no
remaining technical blocker. No provider calls were used for this code review.

## Completed live result

All six calls completed in **879.48 seconds (14m39s)**, within the 900s overall
limit and only about half a second below the 880s worker budget. This is tight
timing, not evidence that six such calls reliably fit fifteen minutes. No deadline
was extended. Usage is **73,156 input + 26,968 output = 100,124 tokens**; output
includes 13,303 reasoning tokens, and input includes 3,456 cached tokens. Do not
add these subsets again. All six counters are complete, no dispatch is pending,
and no retry or fallback occurred. Coding/reviewer-agent usage is unavailable and
outside this ledger. Historical failed-call usage remains unknown.

| Reader condition | Policy | Calls | Tokens | Call time, summed | Financial-draft target |
| --- | --- | ---: | ---: | ---: | --- |
| Operating review only | Packed | 1 | 18,479 | 171.76s | **Fail: false closure** |
| Operating review only | Legacy | 2 | 32,615 | 301.33s | **Fail: false closure** |
| Explicit financial draft | Legacy | 2 | 30,261 | 226.09s | Pass: explicit disclosure recognized |
| Explicit financial draft | Packed | 1 | 18,769 | 180.24s | Pass: explicit disclosure recognized |

There are four target observations, not six: both positive controls pass and both
negative controls fail. The methods agree on the target, but that agreement is
wrong for the negative. Packed costs less in both pairs; that does not establish
semantic reliability or justify changing the default. Its earlier successful
negative observation did not repeat on this minimally changed reader.

Both negative reviews cite the general unaccepted-report label together with:

> The conditional operating package was reviewed only for its operating inputs and calculations.

They treat these as sufficient to cover the protected financial-draft issue.
Neither sentence identifies the financial schedules as unreviewed. Under the
separate financial/operating review-status contract, this is the expected false
closure, not a failure of exact quotation checking. Both positive reviews instead
recognize the explicit added sentence, without needing the literal words “Stage 2”:

> The financial schedules remain an unreviewed draft; their ingestion is not approval.

Every call returned all requested IDs with exact supplied witnesses. One legacy
call produced 6,670 output tokens against its advisory 6,000-token envelope; its
known usage was retained and the aggregate campaign stayed under 350k. This is an
observed per-call overshoot, not a hard-cap guarantee.

### Other issue disagreements, retained separately

- In the negative reader, legacy left `case.gap.gap-fiscal-forecast` unresolved
  while packed marked it covered. Its provider and deterministic findings describe
  **one** gap, not two. A focused Terra/medium review recommends retaining it for
  targeted adjudication: the fiscal operating-income bridge does not by itself
  establish a reviewed fiscal-to-calendar cash-flow/underwriting bridge. Existing
  no-model/no-consensus/no-quote caveats provide partial context, but the scope is
  too indirect for confident closure of this protected compound obligation. This
  is a conservative evaluation judgment, not a new human-reviewed gold label.
- The positive reader did not repair that bridge, yet both methods marked it
  covered. Keep the disagreement visible; do not infer that adding an unrelated
  draft-status sentence resolved it.
- On the positive reader's general pending-acceptance issue, legacy used
  `audit_only_operational` and packed used `reader_covered`. This non-protected
  operational choice is not equivalent to the financial-draft false closure.

## Provenance and integration

Execution revision: `9fb5a9d47876840741b26897d82db3f80d343438`.
Plan SHA-256:
`9c566796da5a053fe8be4c605ed77dd8bcdb3c27825b43d1423f7448a63e564e`.
Complete conservative admission reserve: **287,351 tokens**. The prepared
baseline reader hash is the same as the first paired experiment; both controlled
readers and their expected labels are bound in the new manifest.

Local artifacts are under `reports/NVDA_DISCLOSURE_CONTROLS_20260922/`:
`AUTHORIZATION.md`, `plan_1/plan.json`, `plan_1/run_1/diagnostic.json`, six raw
`call-0-reply.json` through `call-5-reply.json` files, and
`plan_1/supervisor_result.json`. Post-run checks revalidated plan/runtime bindings,
all expected disposition IDs, checked reviews and unchanged source-artifact hashes.
No historical reader, source, usage record or attestation was altered.

Delivery: [PR #21](https://github.com/maluyao002/TradingAgents/pull/21), feature
base `codex/deep-research-v2`. Hosted implementation CI passed on Python 3.10–3.13,
clean locked install and container privacy/import. Final documentation-only
updates do not change the executed code; their hosted checks are recorded in the PR.

## Decision and next deliverable

**Do not start a full end-to-end acceptance run or promote packed to default.**
The approved sequence reached its decision point, but semantic stability was not
demonstrated. More batching benchmarks are not the immediate priority.

Next engineering deliverable:

1. Render a concise financial-review-status disclosure directly from validated
   case state, separately from the operating-package state, with provenance and
   explicit handling of absent/changed review state. Do not depend solely on
   authored prose or a model inferring status from “Needs review.” Retain normal
   factual/coverage review and contradiction checks; do not simply whitelist a
   phrase or downgrade the obligation.
2. Resolve the reader-scope ambiguity between fiscal operating sensitivities and
   a calendar cash-flow/underwriting model. Preserve the protected open issue until
   supported reader disclosure or evidence-backed lifecycle adjudication addresses
   it; changing another paragraph is not resolution evidence.
3. Add offline missing/contradictory/separately-reviewed-status regressions, then
   exercise the actual writer/rendered reader in a small fresh bounded check. The
   synthetic positive sentence is not proof the writer reliably supplies it.

Reconsider end-to-end acceptance only after those gates pass. Financial-case
closure, independent ecosystem evidence, HOOD generality and release approval
remain separate open deliverables. No further live campaign was launched here.
